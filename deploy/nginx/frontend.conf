server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location = /admin {
        return 301 /admin/;
    }

    location = /admin/ {
        try_files /admin.html =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
